__pycache__/
*.py[cod]

# Local SQLite database - never commit your real data
database/*.db

# Environment / secrets
.env
.env.*
!.env.example

# Virtual environments
.venv/
venv/
env/

# Vercel
.vercel/

# npm is build tooling for Tailwind only - the Flask app runs without it.
# The BUILT css (public/static/css/tailwind.css) is committed on purpose so
# Vercel still deploys with no build step.
node_modules/
