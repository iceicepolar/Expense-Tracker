{
  "name": "expense-tracker-css",
  "version": "1.0.0",
  "description": "Build tooling for Tailwind only. The Flask app needs no npm to run.",
  "scripts": {
    "css": "tailwindcss -i public/static/css/tailwind.src.css -o public/static/css/tailwind.css --minify",
    "css:watch": "tailwindcss -i public/static/css/tailwind.src.css -o public/static/css/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  },
  "private": true
}
